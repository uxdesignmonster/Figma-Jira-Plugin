PORT=4000

# Phase 1 (Jira OAuth 3LO) — placeholders only, not used in scaffold.
JIRA_CLIENT_ID=
JIRA_CLIENT_SECRET=
JIRA_REDIRECT_URI=http://localhost:4000/auth/jira/callback

# Phase 1 (token storage)
DATABASE_URL=
